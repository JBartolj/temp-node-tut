// GLOBALS  - NO WINDOW !!!!

// __dirname  - path to current directory
// __filename - file name
// require    - function to use modules (CommonJS)
// module     - info about current module (file)
// process    - info about env where the program is being executed

console.log(__dirname)
const myInteval = setInterval(() => {
  console.log('hello world')
}, 1000)

cancelInterval(myInteval)

setTimeout(() => {
    console.log("TIMEOUT REACHED")
}, 5000)

async function test(paramter, callback) {
    tle neki dela koda 


    tle se neki dela
    callback("","DELA")
}

test( "SERGEJ", (err,result) => {
    if (err) {
        neki nared
    } else {
        pa neki druzga nared
    }
} )

router('/my_results/:userID?', (req,res,next) => {
    if (req.params.userID) {

    } else {

        render

    }
    let myVar
    if (1===1) {
       myVar = "sergej" 



    } else {
        myVar = "jhaneze"
    }
    myVar

    array = ["janez","sergej","janez"]

    const findedName = array.filter(e => e === 'janez')

    array.forEach(e => {
        console.log(e)    
    });

    for(let e of array) {
        console.log(e)
    }

    for(var i = 0; i < array.length; i++) {
        array[i]
    }


   
})